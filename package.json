{
  "name": "trussal-custom-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node build.mjs",
    "deploy:local": "npm run build && cp dist/custom-config.js docker-jitsi-meet/jitsi-web/ && mkdir -p docker-jitsi-meet/jitsi-web/assets && cp -r dist/assets/. docker-jitsi-meet/jitsi-web/assets/",
    "watch": "node build.mjs --watch",
    "dev": "bash scripts/dev.sh",
    "dev:setup": "bash scripts/dev-setup.sh",
    "dev:down": "bash scripts/dev-down.sh",
    "clean": "rm -rf dist",
    "build:strudel-fork": "cd strudel-fork && npm exec --package=pnpm@8 --yes -- pnpm install && npm exec --package=pnpm@8 --yes -- pnpm --filter @strudel/web build && npm exec --package=pnpm@8 --yes -- pnpm --filter @strudel/soundfonts build"
  },
  "devDependencies": {
    "dotenv": "^16.4.0",
    "esbuild": "^0.24.0",
    "ws": "^8.18.0"
  },
  "dependencies": {
    "@strudel/core": "^1.2.6",
    "@strudel/webaudio": "^1.3.0",
    "yjs": "^13.6.0"
  }
}
